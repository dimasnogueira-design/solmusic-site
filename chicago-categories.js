(()=>{if(!matchMedia('(max-width:700px)').matches)return;const cats=document.querySelector('#cats .cats');if(!cats)return;const slides=[...cats.querySelectorAll('.cat')];if(!slides.length)return;const dots=document.createElement('div');dots.className='category-dots';dots.setAttribute('aria-label','Navegação das categorias');slides.forEach((_,i)=>{const b=document.createElement('button');b.type='button';b.className=i===0?'active':'';b.setAttribute('aria-label',`Categoria ${i+1}`);b.addEventListener('click',()=>slides[i].scrollIntoView({behavior:'smooth',block:'nearest',inline:'start'}));dots.appendChild(b)});cats.after(dots);const buttons=[...dots.children];let ticking=false;const sync=()=>{let best=0,dist=Infinity;slides.forEach((s,i)=>{const d=Math.abs(s.getBoundingClientRect().left-cats.getBoundingClientRect().left);if(d<dist){dist=d;best=i}});buttons.forEach((b,i)=>b.classList.toggle('active',i===best));ticking=false};cats.addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(sync);ticking=true}},{passive:true});})();